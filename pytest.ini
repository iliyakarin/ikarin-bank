[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
pythonpath = backend
testpaths = backend/tests

# Route pytest cache to central artifact directory
cache-dir = /tmp/test-results/.pytest_cache